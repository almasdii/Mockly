# Mockly Backend - Week 1

Полнофункциональный backend для платформы Mock Interview, построенный на Java 21 + Spring Boot 3.3.2 + Maven.

## 📋 Что уже сделано

### ✅ Архитектура проекта

Мультимодульный Maven проект с разделением на слои:

- **mockly-api** - REST API контроллеры и конфигурация
- **mockly-core** - Бизнес-логика, сервисы, DTOs
- **mockly-data** - JPA сущности, репозитории, Flyway миграции
- **mockly-security** - JWT аутентификация, Spring Security

### ✅ Реализованные функции

#### Аутентификация (AuthController)
- ✅ `POST /api/auth/register` - Регистрация нового пользователя
- ✅ `POST /api/auth/login` - Вход в систему
- ✅ `POST /api/auth/refresh` - Обновление access token
- ✅ `POST /api/auth/logout` - Выход из системы

#### Управление пользователями (UserController)
- ✅ `GET /api/users/me` - Получить текущего пользователя
- ✅ `PATCH /api/users/me` - Обновить профиль текущего пользователя
- ✅ `GET /api/users/{id}` - Получить пользователя по ID

#### Безопасность
- ✅ JWT токены (HS512 алгоритм)
- ✅ Access Token (1 час)
- ✅ Refresh Token (7 дней, хранится в Redis)
- ✅ BCrypt хеширование паролей (strength=12)
- ✅ Spring Security конфигурация
- ✅ CORS настройки

#### База данных
- ✅ PostgreSQL 16
- ✅ Flyway миграции
- ✅ Таблицы: users, profiles, refresh_tokens, sessions, artifacts, reports, transcripts
- ✅ UUID первичные ключи

#### Инфраструктура
- ✅ Redis для хранения refresh tokens
- ✅ MinIO для хранения файлов (S3-совместимое хранилище)
- ✅ Swagger/OpenAPI документация
- ✅ Глобальная обработка исключений

#### Тесты
- ✅ AuthServiceTest (Mockito) - Unit тесты сервиса аутентификации
- ✅ AuthControllerIntegrationTest (Testcontainers) - Интеграционные тесты

## 🚀 Как запустить

### Предварительные требования

- Java 21 или выше
- Maven 3.8+
- Docker и Docker Compose
- (Опционально) IDE (IntelliJ IDEA, Eclipse, VS Code)

### Шаг 1: Запуск инфраструктуры (Docker)

```bash
# Запустить PostgreSQL, Redis и MinIO
docker-compose up -d

# Проверить статус контейнеров
docker-compose ps
```

Должны запуститься:
- PostgreSQL на порту 5432
- Redis на порту 6379
- MinIO на портах 9000 (API) и 9001 (Console)

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта (или используйте значения по умолчанию):

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mockly
DB_USER=mockly
DB_PASSWORD=mockly_password

REDIS_HOST=localhost
REDIS_PORT=6379

MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=mockly

JWT_SECRET=your-secret-key-must-be-at-least-64-bytes-long-for-hs512-algorithm-to-work-properly
JWT_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=604800000
```

**⚠️ ВАЖНО:** `JWT_SECRET` должен быть минимум 64 байта (512 бит) для HS512 алгоритма!

### Шаг 3: Сборка проекта

```bash
# Из корня проекта
mvn clean install

# Или только компиляция без тестов
mvn clean compile
```

### Шаг 4: Запуск приложения

#### Вариант 1: Через Maven

```bash
cd mockly-api
mvn spring-boot:run
```

#### Вариант 2: Через IDE

1. Откройте проект в IntelliJ IDEA
2. Найдите класс `MocklyApplication` в модуле `mockly-api`
3. Запустите метод `main()`

#### Вариант 3: Через JAR

```bash
cd mockly-api
mvn package
java -jar target/mockly-api-1.0.0-SNAPSHOT.jar
```

Приложение запустится на `http://localhost:8080`

## 🧪 Как проверить

### 1. Проверка здоровья приложения

```bash
# Health check
curl http://localhost:8080/actuator/health
```

### 2. Swagger UI

Откройте в браузере:
```
http://localhost:8080/swagger-ui.html
```

Здесь вы можете:
- Просмотреть все API endpoints
- Протестировать API прямо из браузера
- Увидеть схемы запросов/ответов

### 3. Тестирование API через curl

#### Регистрация пользователя

```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "displayName": "Test User",
    "role": "CANDIDATE"
  }'
```

Ответ:
```json
{
  "accessToken": "eyJhbGciOiJIUzUxMiJ9...",
  "refreshToken": "eyJhbGciOiJIUzUxMiJ9...",
  "userId": "uuid-here"
}
```

#### Вход в систему

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

#### Получение текущего пользователя

```bash
curl -X GET http://localhost:8080/api/users/me \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

#### Обновление профиля

```bash
curl -X PATCH http://localhost:8080/api/users/me \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "displayName": "Updated Name",
    "level": "Senior"
  }'
```

#### Обновление токена

```bash
curl -X POST http://localhost:8080/api/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{
    "refreshToken": "YOUR_REFRESH_TOKEN"
  }'
```

### 4. Запуск тестов

```bash
# Все тесты
mvn test

# Только unit тесты
cd mockly-core
mvn test

# Только интеграционные тесты
cd mockly-api
mvn test
```

### 5. Проверка базы данных

```bash
# Подключение к PostgreSQL
docker exec -it mockly-postgres psql -U mockly -d mockly

# Просмотр таблиц
\dt

# Просмотр пользователей
SELECT * FROM users;

# Просмотр профилей
SELECT * FROM profiles;
```

### 6. Проверка Redis

```bash
# Подключение к Redis
docker exec -it mockly-redis redis-cli

# Просмотр всех ключей
KEYS *

# Просмотр refresh token
GET refresh_token:USER_ID
```

## 📁 Структура проекта

```
mockly-backend/
├── pom.xml                          # Root POM
├── docker-compose.yml               # Docker инфраструктура
├── .env.example                     # Пример переменных окружения
├── .gitignore
│
├── mockly-api/                      # API слой
│   ├── src/main/java/
│   │   └── com/mockly/
│   │       ├── MocklyApplication.java
│   │       └── api/
│   │           ├── controller/     # REST контроллеры
│   │           ├── config/         # Конфигурация (Swagger)
│   │           └── exception/     # Обработчики исключений
│   └── src/main/resources/
│       └── application.yml         # Конфигурация приложения
│
├── mockly-core/                     # Бизнес-логика
│   └── src/main/java/
│       └── com/mockly/core/
│           ├── dto/                # Data Transfer Objects
│           ├── service/             # Сервисы
│           └── exception/           # Исключения
│
├── mockly-data/                     # Слой данных
│   └── src/main/java/
│       └── com/mockly/data/
│           ├── entity/              # JPA сущности
│           └── repository/         # Репозитории
│   └── src/main/resources/
│       └── db/migration/            # Flyway миграции
│
└── mockly-security/                  # Безопасность
    └── src/main/java/
        └── com/mockly/security/
            ├── config/              # Security конфигурация
            ├── jwt/                 # JWT провайдер и фильтр
            └── service/             # UserDetailsService
```

## 🔑 Ключевые endpoints

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| POST | `/api/auth/register` | Регистрация | ❌ |
| POST | `/api/auth/login` | Вход | ❌ |
| POST | `/api/auth/refresh` | Обновление токена | ❌ |
| POST | `/api/auth/logout` | Выход | ❌ |
| GET | `/api/users/me` | Текущий пользователь | ✅ |
| PATCH | `/api/users/me` | Обновить профиль | ✅ |
| GET | `/api/users/{id}` | Пользователь по ID | ✅ |

## 🛠 Технологии

- **Java 21** - Язык программирования
- **Spring Boot 3.3.2** - Фреймворк
- **Spring Security** - Безопасность
- **Spring Data JPA** - Работа с БД
- **PostgreSQL 16** - База данных
- **Redis 7** - Кэш и refresh tokens
- **MinIO** - S3-совместимое хранилище
- **Flyway** - Миграции БД
- **JWT (JJWT 0.12.3)** - Токены
- **Swagger/OpenAPI** - API документация
- **Testcontainers** - Интеграционные тесты
- **Mockito** - Unit тесты

## 📝 Следующие шаги (Week 2+)

- [ ] Управление сессиями интервью
- [ ] Загрузка и обработка артефактов
- [ ] Генерация отчетов
- [ ] WebRTC интеграция
- [ ] Обработка транскриптов
- [ ] Аналитика и метрики

## 🐛 Troubleshooting

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте, что Docker контейнеры запущены: `docker-compose ps`
2. Проверьте логи: `docker-compose logs`
3. Убедитесь, что порты 5432, 6379, 9000 свободны

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверьте, что PostgreSQL запущен: `docker ps | grep postgres`
2. Проверьте переменные окружения в `application.yml`
3. Попробуйте подключиться вручную: `docker exec -it mockly-postgres psql -U mockly`

### Проблема: JWT ошибки

**Решение:**
1. Убедитесь, что `JWT_SECRET` минимум 64 байта
2. Проверьте формат токена в заголовке: `Authorization: Bearer TOKEN`

### Проблема: Тесты не проходят

**Решение:**
1. Убедитесь, что Docker запущен (для Testcontainers)
2. Проверьте версию Java: `java -version` (должна быть 21+)
3. Очистите и пересоберите: `mvn clean test`

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи приложения
2. Логи Docker контейнеров
3. Swagger UI для проверки API
4. Health endpoint: `/actuator/health`

---

**Готово к использованию! 🎉**

