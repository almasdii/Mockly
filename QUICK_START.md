# Быстрый старт

Краткое руководство по запуску проекта за 5 минут.

## 1. Запуск инфраструктуры

```bash
docker-compose up -d
```

Ждите ~30 секунд пока все сервисы запустятся.

## 2. Проверка сервисов

```bash
docker-compose ps
```

Все сервисы должны быть в статусе "Up".

## 3. Запуск приложения

```bash
cd mockly-api
mvn spring-boot:run
```

Или через IDE: запустите `MocklyApplication.java`

## 4. Проверка работы

Откройте в браузере:
- **Swagger UI**: http://localhost:8080/swagger-ui.html
- **Health**: http://localhost:8080/actuator/health

## 5. Первый тест

### Регистрация пользователя через Swagger:

1. Откройте http://localhost:8080/swagger-ui.html
2. Найдите `POST /api/auth/register`
3. Нажмите "Try it out"
4. Вставьте JSON:
```json
{
  "email": "test@example.com",
  "password": "password123",
  "role": "CANDIDATE",
  "displayName": "Test User"
}
```
5. Нажмите "Execute"
6. Скопируйте `accessToken` из ответа
7. Нажмите кнопку **Authorize** (вверху справа)
8. Введите: `Bearer <ваш-token>`
9. Теперь можете тестировать другие endpoints!

## Готово! 🎉

Теперь вы можете:
- Создавать сессии
- Загружать артефакты
- Генерировать отчеты
- Подключаться к WebSocket

Подробная документация в [README.md](README.md)

